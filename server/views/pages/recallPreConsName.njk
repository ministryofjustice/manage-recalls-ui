{% extends "../partials/layout.njk" %}

{% set pageTitle = applicationName + " - Book a recall - What is the main name on the pre-cons?" %}

{% block beforeContent %}
    {{ govukBackLink({
        text: "Back",
        href: backLinkUrl("/find-person?nomsNumber=" + person.nomsNumber, urlInfo)
    }) }}
{% endblock %}

{% block content %}

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            {% if errors %}
                {{ govukErrorSummary({
                    titleText: "There is a problem",
                    errorList: errors.list,
                    attributes: {
                        'data-qa': 'error-list'
                    }
                }) }}
            {% endif %}
            <span class="govuk-caption-l">Book a recall for {{ person.firstName }} {{ person.lastName }}</span>

            <form novalidate action="{{ formActionUrl('pre-cons-name', urlInfo) }}" method="post">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
                <div class='govuk-body govuk-form-group govuk-!-margin-bottom-8'>

                    {% set yesDetailHtml %}
                        {{ govukInput({
                            name: "previousConvictionMainName",
                            id: "previousConvictionMainName",
                            label: {
                                text: "What is the main name?"
                            },
                            value: formValues.previousConvictionMainName,
                            classes: "govuk-input--width-10",
                            errorMessage: errorMessage(errors.previousConvictionMainName)
                        }) }}
                    {% endset -%}
                    {{ govukRadios({
                        idPrefix: "hasOtherPreviousConvictionMainName",
                        name: "hasOtherPreviousConvictionMainName",
                        fieldset: {
                            legend: {
                                text: "What is the main name on " + person.firstName + " " + person.lastName + "'s pre-cons?",
                                classes: "govuk-fieldset__legend--l",
                                isPageHeading: true
                            }
                        },
                        items: [
                            {
                                value: "NO",
                                text: person.firstName + " " + person.lastName,
                                checked: formValues.hasOtherPreviousConvictionMainName == 'NO'
                            },
                            {
                                value: "YES",
                                text: "Other name",
                                checked: formValues.hasOtherPreviousConvictionMainName == 'YES',
                                conditional: {
                                    html: yesDetailHtml
                                }
                            }
                        ],
                        errorMessage: errorMessage(errors.hasOtherPreviousConvictionMainName)
                    }) }}
                </div>
                <div>
                    <button class="govuk-button"
                            data-qa='continueButton'>
                        Continue
                    </button>
                </div>
            </form>
        </div>
    </div>

{% endblock %}

