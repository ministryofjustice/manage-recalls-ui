{% extends "../partials/layout.njk" %}

{% set pageTitle = applicationName + " - Book a recall - Upload documents" %}

{% block beforeContent %}
    {{ govukBackLink({
        text: "Back",
        href: backLinkUrl("probation-officer", urlInfo),
        attributes: {
            'data-qa': 'backLinkUploadDocuments'
        }
    }) }}
{% endblock %}

{% block content %}

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            {% if errors %}
                {{ govukErrorSummary({
                    titleText: "There is a problem",
                    errorList: errors.list,
                    attributes: {
                        'data-qa': 'error-list'
                    }
                }) }}
            {% endif %}
            <span class="govuk-caption-l">Book a recall for {{ person.firstName }} {{ person.lastName }}</span>
            <h1 class="govuk-heading-l">
                Upload documents
            </h1>
        </div>
    </div>

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-desktop">
            <form novalidate action="{{ formActionUrl('upload-documents', urlInfo, csrfToken) }}" method="post" enctype="multipart/form-data">
                <div class="govuk-form-group">
                    {% for documentCategory in recall.documentCategories %}
                        <h2 class="govuk-heading-m govuk-!-margin-bottom-3">{{ documentCategory.label }}{% if documentCategory.required != true %} (optional){% endif %}</h2>
                        {% if documentCategory.uploaded.length %}
                            <input type='hidden' name='{{ documentCategory.name}}' value='existingUpload'>
                            <div class='govuk-body govuk-!-margin-bottom-4'>
                                <ul class='govuk-list'>
                                {% for uploaded in documentCategory.uploaded %}
                                    <li><a href='{{ uploaded.url }}' target='_blank' class='govuk-link' data-qa='uploadedDocument-{{ documentCategory.name}}'>{{ uploaded.fileName }}</a></li>
                                {%endfor %}
                                </ul>
                            </div>
                        {% endif %}
                        {{ govukFileUpload({
                            id: documentCategory.name,
                            name: documentCategory.name,
                            label: {
                                text: 'Upload a ' + documentCategory.label,
                                classes: 'govuk-visually-hidden'
                            },
                            attributes: removeUndefinedFromObject({
                                accept: allowedDocumentFileExtensionList(),
                                multiple: true if documentCategory.multiple == true else undefined
                            }),
                            errorMessage: errorMessage(errors[documentCategory.name])
                        }) }}
                    {%  endfor %}
                </div>
                <button class="govuk-button" data-qa='continueButton'>
                    Continue
                    </button>
            </form>
        </div>
    </div>

{% endblock %}
