{% extends "../partials/layout.njk" %}
{% from "govuk/components/table/macro.njk" import govukTable %}
{%- from "moj/components/search/macro.njk" import mojSearch -%}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% set pageTitle = applicationName + " - Recall is authorised" %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            <div class="govuk-panel govuk-panel--confirmation">
                <h1 class="govuk-panel__title">
                    Recall for {{ person.firstName }} {{ person.lastName }} is authorised
                </h1>
                <div class="govuk-panel__body">
                    Recall ID:<br><strong>{{recall.recallId}}</strong>
                </div>
            </div>
            <p class='govuk-body'>
                tbc...
            </p>
            <p class='govuk-body'>
                <a href="/get-revocation-order?recallId={{ recall.recallId }}" role='button' class="govuk-button" data-module="govuk-button"
                   data-qa="getRevocationOrderButton" download>
                    Download revocation order
                </a>
            </p>

        </div>
    </div>
{% endblock %}
